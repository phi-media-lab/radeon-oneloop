# Tests

Add focused tests before long formal jobs. Initial priorities are the dataset
schema, camera-key ordering, 12-DoF action ordering, gripper conversion,
phase-balanced sampler, single-Radeon assertion, and checkpoint reload.
